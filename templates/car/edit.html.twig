{% extends 'base.html.twig' %}

{% block header_title %}<h1>Modifier <strong>{{ car.brand }}</strong></h1>{% endblock %}

{% block body %}
    <div class="row">
        {{ form_start(form, {'action' : path('edit_car', {'id' : car.id})}) }}
        {{ form_widget(form) }}

        <button type="submit" class="btn btn-success pull-right">Modifier</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
